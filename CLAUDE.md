# CLAUDE.md

이 저장소에서 작업할 때 지키는 규칙. 제품 방향은 `PROJECT.md`, 데이터 제공자
선정 과정은 `docs/decisions/01-data-source.md` 참고.

## 절대 규칙

- **금액·비율 계산에는 `BigDecimal`만 쓴다. `double`/`float` 금지.** 환율까지
  곱해지는 계산이라 부동소수점 오차가 실제로 드러난다. ArchUnit이 도메인
  패키지에서 이를 강제한다.
- **서비스 계층은 DB만 읽는다.** Massive·Twelve Data 같은 외부 API는 배치
  수집 계층에서만 호출하고, 서비스·컨트롤러에서 직접 호출하지 않는다.
- **테스트 없는 계산 로직을 추가하지 않는다.** 계산 로직에는 반드시 대응하는
  테스트가 있어야 하고, 최소 1개는 손계산으로 검증한 값이어야 한다.
- **종목 추천·투자 판단·미래 예측성 문구를 생성하지 않는다.** ("이 종목을
  사세요", "좋은 종목입니다", "배당이 오를 겁니다" 같은 표현 금지)

## 도메인 규칙

- **배당락일은 T+1 규칙을 따른다.** 2024-05-28 SEC T+1 결제 전환 이후 미국
  배당락일과 기준일은 대부분 같은 날이다 — "배당락일 전날 종가 기준으로
  보유"해야 배당을 받는다. 인터넷 자료에 T+2 시절 규칙이 섞여 있으니 그걸로
  구현하지 않는다. 근거는 SEC 원문으로 `docs/decisions/`에 남긴다.
- **세금은 계산의 마지막 단계에서만 적용한다.** 미국 원천징수 15%는
  `US_WITHHOLDING_TAX_RATE`로 명명하고, 한국 주식 15.4%와 절대 혼동하지 않는다.
- **환율 적용 시점은 "배당 지급일 기준"으로 고정한다.** 바꿀 일이 생기면
  반드시 `docs/decisions/`에 문서화한다.
- **특별배당은 정기 배당과 분리한다.** 지급 이력에서 정기 주기를 추론하고,
  벗어나는 지급은 특별배당 후보로 분류한다 — 자동으로 합산하지 않는다.

## 데이터

- 배당·분할 이력 = Massive, 주가 시계열 = Twelve Data (`docs/decisions/01-data-source.md`).
- 시계열에는 구멍(누락된 분기, null 필드, 분할 전후 단위 불일치, 제공자별로
  다른 커버 기간)이 있다는 걸 전제로 짠다. 감지하면 로그를 남기고 화면에
  "데이터 불완전"을 표시한다 — 조용히 넘어가지 않는다.
- 가격은 "정확히 이 날짜"가 아니라 "이 날짜 이전·근처의 가장 가까운 값"을
  찾도록 조회한다.

## 커밋 메시지

```
feat: 배당률 변화 기여도 분해 구현

Plan: docs/decisions/03-yield-decomposition.md
Catch: AI가 특별배당을 정기 배당에 합산 → 테스트 실패 → 제외 로직 추가
```

`Catch:` 줄에는 AI가 실제로 틀렸던 부분과 어떻게 잡았는지 적는다. 없는 날은
생략한다.
