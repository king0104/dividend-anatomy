# 배당 지표 해부 도구 (가칭)

> 배당 종목의 숫자가 **어디서 왔는지 분해해서** 보여주는 도구.
> 숫자를 보여주는 계산기가 아니라, 숫자를 읽는 법을 알려주는 도구.

---

## 1. 왜 만드는가

### 문제

배당 투자를 시작하려는 초보는 벽을 여러 겹 만난다.

| 벽 | 기존 서비스가 해결하는가 |
|---|---|
| 얼마 들어오는지 모름 | 해결됨 (계산기 다수) |
| 언제 들어오는지 모름 | 해결됨 (배당 캘린더 다수) |
| 세금 얼마 떼는지 모름 | 해결됨 |
| **이 숫자가 왜 이런지 모름** | **거의 안 해줌** |
| **이 종목이 위험한지 모름** | **거의 안 해줌** |
| 뭘 사야 할지 모름 | 규제상 불가 (우리도 안 함) |

기존 서비스(아는자산, bebooja, 스노우볼, 이자 앱 등)는 **숫자를 나열하되 해석하지 않는다.**
배당성향이 95%라는 게 무슨 뜻인지, 배당률 8%가 왜 위험 신호일 수 있는지는 사용자 몫으로 남긴다.
초보에게는 그 숫자가 있으나 없으나 같다.

### 초보가 실제로 돈을 잃는 경로

배당률 = 배당금 ÷ 주가.
따라서 **주가가 반토막 나면 배당률은 두 배로 뛴다.**
초보는 이를 "좋은 종목"으로 읽고 매수하고, 곧 배당 삭감과 추가 주가 하락을 겪는다.

이 도구는 그 함정 하나를 정면으로 다룬다.

### 목표

- **1차 목표**: 만드는 사람이 배당 투자 구조를 이해한다
- **2차 목표**: 같은 벽 앞에 선 사람에게 쓸모가 있다
- **비목표**: 기존 서비스와의 경쟁, 사용자 수, 수익화

---

## 2. 핵심 컨셉

### 하지 않는 것

- 종목 추천 ("이걸 사세요")
- 투자 판단 ("이건 좋은 종목입니다")
- 미래 예측 ("앞으로 배당이 오를 겁니다")

### 하는 것

**숫자의 출처를 분해해서 보여준다.**

```
알트리아 (MO)
시가배당률 8.2%   ⚠️ 확인 필요

  배당률이 3년 전 4.1% → 현재 8.2%
  ├─ 배당금 증가 기여:  +8%
  └─ 주가 하락 기여:   -22%
  → 이 배당률 상승의 대부분은 주가 하락에서 왔습니다.

  배당성향 78%
  → 번 돈의 78%를 배당으로 지급 중.
     100%를 넘으면 이익보다 많이 배당하는 상태입니다.

  ✓ 50년 이상 연속 배당 증가
```

판정은 **규칙 기반 계산**이 하고, 문장은 계산 결과를 감싸는 표현 계층일 뿐이다.
설명 텍스트를 미리 써두지 않는다. 데이터에서 뽑아낸다.

---

## 3. 대상 사용자

**"배당 투자를 시작하려는데 뭘 봐야 할지 모르는 사람"**

- 첫 사용자는 개발자 본인
- 티커를 모르는 상태에서 시작해도 되는 UX
- 용어를 안다고 전제하지 않음

---

## 4. 기능 명세

### 4.1 종목 목록 (필수)

배당킹(50년 이상 연속 배당 증가) 약 57개 종목을 표로 제공.

| 컬럼 | 비고 |
|---|---|
| 티커 / 회사명 | |
| 현재 주가 | USD / KRW 토글 |
| 시가배당률 | 경고 플래그 함께 표시 |
| 연속 배당 증가 연수 | |
| 지급 주기 | 분기 / 월 |

**유니버스 선정 기준**
- 배당킹은 "50년 이상 연속 배당 증가"라는 객관적 기준으로 정해진 목록 → 추천이 아님
- 57개면 하루 1회 갱신 시 API 호출량이 충분히 여유로움
- 단, 월배당 종목(리얼티인컴 등)이 빠지므로 월별 차트가 특정 월에 몰릴 수 있음
  → **Day 1~2에 실제 지급 월 분포를 확인한 뒤 보완 여부 결정** (`docs/decisions/02-universe.md`)

### 4.2 종목 상세 — 지표 해부 (핵심)

이 프로젝트의 정체성. 아래 계산 엔진의 결과를 표시.

1. **배당률 변화 기여도 분해** — 배당금 요인 vs 주가 요인
2. **배당 지속성 지표** — 성장률 둔화, 삭감 이력, 변동성
3. **배당성향 해석** — 이익 대비 배당 부담
4. **경고 플래그** — 규칙에 걸리면 표시

### 4.3 월별 예상 현금흐름 (필수)

관심 종목에 수량을 입력하면 12개월 막대그래프.

- 세전 / 세후 토글 (미국 원천징수 15%)
- USD / KRW 토글
- 막대 클릭 시 해당 월 구성 종목 내역

**의도된 발견**: 분기 배당이라 특정 월에 몰린다는 사실을 사용자가 눈으로 확인하게 함.

### 4.4 목표 역산 (여유 시)

"월 30만원 배당" 입력 → 필요 원금과 주식 수 역산.
의도: 원금 규모에 대한 현실 감각.

### 4.5 범위 밖 (2주 내 하지 않음)

로그인 · 알림 · 실시간 시세 · ETF · 종목 추천 · 포트폴리오 자동 조합 · 배당 캘린더

> 배당 캘린더는 기존 서비스에 이미 충분히 존재하고 이 프로젝트의 핵심이 아니므로 제외.

---

## 5. 계산 엔진 명세

**이 프로젝트의 개발 무게는 전부 여기 실린다.**
화면은 얇게, 계산은 두껍게.

### 5.1 배당률 변화 기여도 분해

배당률 = 연간 배당금 ÷ 주가.
기준 시점 대비 현재의 배당률 변화를 분자(배당금) 효과와 분모(주가) 효과로 분리.

**설계 결정 필요 사항** → `docs/decisions/`에 기록
- 기준 시점을 언제로 잡을 것인가 (3년 전? 5년 전?)
- 분기 배당의 연환산 방식 (직전 4회 합? TTM?)
- 주식 분할 반영 방식
- 반올림 방향과 자릿수

### 5.2 배당 지속성 지표

| 지표 | 정의 | 설계 결정 |
|---|---|---|
| 성장률 둔화 | 최근 3년 CAGR vs 10년 CAGR | 기간 선택 근거 |
| 삭감 이력 | 시계열에서 감소 구간 탐지 | 특별배당 제외 처리 |
| 변동성 | 배당금 증감률의 표준편차 | 표본 기간 |

**이 지표들은 기성품이 아니라 직접 정의한다.** 정의 근거를 문서화하는 것이 산출물의 일부.

### 5.3 이상치 제거

특별배당(일회성)이 섞이면 배당률과 성장률이 왜곡된다.

- 정기 배당 주기를 지급 이력에서 추론
- 주기에서 벗어난 지급을 특별배당 후보로 분류
- 제외 여부와 근거를 표시

> **AI가 거의 확실히 놓치는 지점.** 기본적으로 전부 합산해버린다.
> 테스트를 먼저 작성해서 잡아낼 것.

### 5.4 시계열 정합성 검증

배당 데이터에는 구멍이 많다.

- 특정 분기 누락
- `paymentDate` 가 null
- 주식 분할 전후 금액 단위 불일치

감지 → 로그 → 화면에 "데이터 불완전" 표시. 조용히 넘어가지 않는다.

### 5.5 세금 계산

- 미국 주식 배당은 **미국에서 15% 원천징수** 후 입금
- 한·미 조세조약에 따라 국내 추가 납부는 없음
- 단, 연간 금융소득 2,000만원 초과 시 금융소득종합과세 합산 대상

**구현 범위**: 원천징수 15%까지만 계산. 종합과세는 안내 문구로 처리.
**주의**: 한국 주식의 15.4%와 혼동 금지. 상수명을 `US_WITHHOLDING_TAX_RATE` 로 명확히.

### 5.6 환율

- 적용 시점을 **"배당 지급일 기준"** 으로 고정하고 문서화
- 변경 시 반드시 `docs/decisions/` 갱신
- 미래 지급분은 현재 환율 적용 + "환율 변동에 따라 달라짐" 명시

---

## 6. 배당 규칙 — 반드시 테스트로 고정할 것

### T+1 전환 (중요)

미국 시장은 **2024년 5월 28일부터 T+1 결제**로 전환됨 (SEC 최종 규칙).

그 결과:
- **배당락일과 기준일이 일반적으로 같은 날**
- 2026년 미국 현금 배당의 약 98.9%가 배당락일 = 기준일

**한 줄 규칙**: 배당락일 **전날 종가 기준으로 보유**하고 있어야 배당을 받는다. 배당락일 당일에 매도해도 받는다.

### 왜 이게 중요한가

인터넷에는 **T+2 시절 규칙과 T+1 규칙이 뒤섞여** 있다. 2026년에 갱신된 문서에도 옛 규칙이 남아 있다.
→ **AI는 이 뒤섞인 데이터를 학습했으므로 옛 규칙으로 구현할 가능성이 높다.**
→ 이 프로젝트에서 가장 가치 있는 `Catch:` 기록이 여기서 나올 것.

근거는 블로그가 아니라 **SEC 원문**을 인용해 `docs/decisions/` 에 남길 것.

### 지급일

지급은 통상 배당락일 후 2~4주. 정확한 날짜는 배당 선언 시 발표.
API가 `paymentDate` 를 주지 않는 종목은 근사값 사용 + "예상" 표시 + 판단 근거 문서화.

---

## 7. 데이터

### 필요 데이터

| 데이터 | 용도 | 확보 상태 |
|---|---|---|
| 배당 이력 (배당락일, 지급일, 주당배당금) | 캘린더, 현금흐름 | Day 1 확인 |
| 주가 시계열 | **기여도 분해** | Day 1 확인 (필수) |
| 배당성향 / 재무 지표 | 지속성 판정 | Day 1 확인 |
| 현재 주가 | 배당률 | 확보 가능 |
| USD/KRW 환율 | 원화 환산 | 확보 가능 |

### 제공자

**주력: Finnhub**
- 무료 플랜 분당 60콜, 신용카드 불필요, 가입 즉시 토큰 발급
- 개인·비상업 용도 명시 → 포트폴리오 배포가 이 범위에 부합
- 배당 이력 장기 제공

**후보에서 제외한 것**

| 제공자 | 제외 사유 |
|---|---|
| FMP | 데이터 **표시·재배포 시 별도 라이선스 계약 필요** → 공개 배포에 부적합 |
| yfinance | Yahoo 엔드포인트 비공식 스크래핑, 약관 위반 소지 |
| Alpha Vantage | 무료 하루 25콜로 부족 |

**예비: Twelve Data** (무료 하루 800콜, 미국 시장 커버)

> ⚠️ 무료 API 정책은 자주 바뀐다. 위 내용은 **가입 후 실제 응답과 현재 약관을 직접 확인**할 것.

### 아키텍처 원칙

```
[외부 API] → (일 1회 배치) → [DB] → [서비스] → [화면]
```

- 서비스 계층은 **DB만 읽는다**. 외부 API를 직접 호출하지 않는다.
- 제공자별 어댑터로 격리 → 교체 비용 최소화
- 효과: 호출량 문제 소멸, API 장애 시에도 서비스 동작, 제공자 교체가 수 시간 내 가능

---

## 8. 기술 스택

| 영역 | 선택 |
|---|---|
| 언어 / 프레임워크 | Java + Spring Boot |
| 빌드 | Gradle |
| DB | (미정 — Day 1 결정) |
| 테스트 | JUnit + AssertJ + **ArchUnit** |
| 배포 | 보유 서버 |
| AI 도구 | Claude Code |

### ArchUnit 규칙 (필수)

```
도메인 패키지에서 double, float 사용 금지 → BigDecimal 강제
```

돈 계산에 환율까지 곱해지므로 부동소수점 오차가 실제로 드러난다.
AI가 `double` 을 쓰는 순간 빌드가 깨지도록 강제.

---

## 9. 개발 프로세스 — AI 활용

**이 프로젝트의 또 다른 산출물.** 결과물이 아니라 과정을 증거로 남긴다.

> **핵심 명제**
> AI 도구 목록이 아니라, **AI 산출물을 검증한 방법**을 기록한다.
> 도구는 누구나 켤 수 있다. 갈리는 것은 판단이다.

세 층으로 구성한다.

---

### 9.A 판단 — 차별점

#### 9.A.1 도입하지 않은 것과 그 이유

`docs/decisions/00-ai-harness.md` 에 기록.

| 안 쓴 것 | 이유 |
|---|---|
| 커스텀 서브에이전트 | 컨텍스트 분리 필요가 발생하지 않음. 특히 **구현을 분리하면 메인 대화에서 코드 맥락이 사라져 개발자 본인이 흐름을 놓친다** |
| Skills | 다수의 지식 묶음을 컨텍스트 낭비 없이 관리하는 구조. 이 프로젝트 지침은 CLAUDE.md 한 페이지로 충분해 분할 이득 없음 |
| 하네스 / 멀티 에이전트 오케스트레이션 | 반복 작업에서 값을 하는 도구. 이 프로젝트는 일회성 |
| MCP 서버 자체 제작 | 시간 대비 효용 낮음 |
| GitHub Actions 자동 리뷰 | 다음 프로젝트로 |

**기록 시 숫자를 넣을 것.** 소스 파일 수, 최대 컨텍스트 사용률 등을 근거로 제시하면
"감으로 판단"이 아니라 "관찰 후 판단"이 된다.

> 유행하는 것을 쓰는 것보다 **안 쓴 이유를 대는 것이 어렵다.**
> 면접에서 "서브에이전트는 왜 안 쓰셨어요?"에 그대로 답이 된다.

#### 9.A.2 도구의 성격을 구분해서 선택했다는 것

> Hooks는 **결정론적**(매번 예외 없이 실행), CLAUDE.md와 Skills는 **확률적**(모델이 판단해 사용).
> 컴파일·테스트는 반드시 실행돼야 하므로 hooks로 강제했다.
> CLAUDE.md에 "테스트를 돌려라"라고 쓰는 것은 제안이고, hook은 보장이다.

이 한 문단이 도구 다섯 개 나열보다 이해도를 잘 보여준다.

#### 9.A.3 실패 예측 — `docs/ai-predictions.md`

**Day 2에 작성. 구현 전에 적어야 의미가 있다.**

| # | 예측 | 근거 | 결과 |
|---|---|---|---|
| 1 | 배당락일을 T+2 시절 규칙(기준일 1영업일 전)으로 구현할 것 | 인터넷에 옛 규칙과 새 규칙이 혼재 | ? |
| 2 | 특별배당을 정기 배당에 합산할 것 | 구분 로직이 자명하지 않음 | ? |
| 3 | 금액에 `double` 을 쓸 것 | 일반적 관성 | ? |
| 4 | 주식 분할 전후 배당금 단위 불일치를 무시할 것 | 데이터에 명시되지 않음 | ? |
| 5 | 미국 15%와 한국 15.4%를 혼동할 것 | 두 숫자가 비슷함 | ? |

- 사후 회고면 "정리했다"에 그치지만, **사전 예측이면 "도메인을 이해했기에 AI의 실패 지점을 예측할 수 있었다"는 증명이 된다**
- 결과 칸을 채우면 적중률이 나온다. 맞은 것은 테스트가 잡아준 것, 틀린 것은 "예상보다 AI가 잘하는 영역"이라는 발견. **둘 다 기록 가치가 있다**
- 이 프로젝트라서 가능하다. 계산 규칙이 명확하고 함정이 특정 가능하기 때문. CRUD 프로젝트에서는 나오지 않는다

---

### 9.B 통제 장치 — 실제로 일하는 것들

각각에 **"이 프로젝트에서 왜 필요했는가"** 가 붙어야 의미가 생긴다.

| 장치 | 이 프로젝트에서의 명분 |
|---|---|
| **ArchUnit** (`double` 금지) | 돈 계산에 환율까지 곱해져 부동소수점 오차가 실제로 드러남 |
| **Hooks** | 컴파일·테스트가 **반드시** 실행돼야 하는 결정론적 요구 |
| **테스트 우선** | AI 출력이 맞는지 판단할 유일한 수단 |
| **Plan mode** | 반올림 방향·기준 시점 같은 애매한 지점을 AI가 먼저 질문하게 유도 |
| **`/spec` 커맨드** | 스펙 없이 구현시키면 AI가 반올림 방향을 임의로 정함 (돈 계산에서 치명적) |

#### CLAUDE.md

Claude Code에게 초안을 시키고 사람은 검토. 최소 포함 항목:

```
- 금액은 반드시 BigDecimal. double/float 금지
- 환율 적용 시점은 "배당 지급일 기준" (변경 시 문서화)
- 세금은 계산 마지막 단계에서만 적용
- 테스트 없는 계산 로직 추가 금지
- 종목 추천 / 투자 조언성 문구 생성 금지
```

작업 중 AI가 엉뚱하게 짤 때마다 규칙을 추가한다.
**이 파일의 커밋 히스토리가 "AI를 통제했다"는 가장 직접적인 증거.**

#### Hooks (Day 1~2에 반드시 설정)

`.claude/settings.json` — 프로젝트에 커밋해서 설정 자체를 저장소에 남긴다.

```
PostToolUse (Write|Edit) → ./gradlew compileJava -q   (동기, 빠름)
Stop (세션 종료)          → ./gradlew test             (async: true)
```

Day 2~3에 추가 검토: **API 키 하드코딩 차단** (PreToolUse, exit 2)
→ Finnhub 토큰을 다루므로 실제 위험이 있어 명분이 확실함

주의:
- exit 0 = 통과, **exit 2 = 차단**, exit 1 = 아무 효과 없음 (가장 흔한 함정)
- PostToolUse는 도구가 이미 실행된 뒤이므로 exit 2로 되돌릴 수 없음. 피드백 용도
- 몇 초 이상 걸리면 `"async": true`

> 나중에 걸면 그동안 놓친 실수를 소급 발견할 수 없다.

#### 슬래시 커맨드 (3개만)

`.claude/commands/` — 워크플로우에서 **실제로 반복되는 지점**만.

| 커맨드 | 하는 일 |
|---|---|
| `/spec` | 지표 스펙 템플릿 생성. 계산식·기준시점·반올림·예외를 반드시 채우게 강제 |
| `/defect` | 실패 순간을 `docs/ai-defects/` 에 즉시 기록 (나중에 몰아 쓰면 기억이 흐려짐) |
| `/verify` | 계산 결과를 손계산 케이스와 대조 |

#### 커밋 메시지 규칙 (Day 1부터 — 소급 적용 불가)

```
feat: 배당률 변화 기여도 분해 구현

Plan: docs/decisions/03-yield-decomposition.md
Catch: AI가 특별배당을 정기 배당에 합산 → 테스트 실패 → 제외 로직 추가
```

`Catch:` 줄이 핵심. 쌓이면 README 회고를 따로 쓸 필요가 없어진다. 없는 날은 생략.

#### 세션 관리

기능 하나 끝나면 `/clear`. 컨텍스트가 길어지면 판단력이 떨어진다.
컨텍스트 사용률을 몇 번 메모해두면 9.A.1의 근거 숫자가 된다.

---

### 9.C 워크플로우 — 방법론

이 프로젝트의 반복 단위는 **"지표 하나 추가하기"** 하나뿐이다.
(기여도 분해 / 성장률 둔화 / 삭감 이력 / 변동성 / 이상치 제거 — 전부 같은 모양)

**작업 단위를 식별하고 거기에 맞춰 순서를 설계한 것**이 방법론의 실체다.
남의 템플릿이 아니라 이 프로젝트에서 도출된 순서이므로 설명이 가능하다.

`docs/workflow.md`

```
지표 추가 워크플로우

1. /spec   스펙 작성 → docs/specs/{지표명}.md
           계산식 · 기준 시점 · 반올림 방향 · 예외 케이스 명시

2. 예측    AI가 틀릴 지점을 docs/ai-predictions.md 에 먼저 기록

3. /test   스펙 기반 테스트 먼저 작성
           손계산으로 검증한 케이스 최소 2개 포함

4. plan    Plan mode로 구현 계획 요청
           계획을 반드시 한 군데 수정 (고칠 데가 안 보이면 아직 이해 못 한 것)
           AI 판단에 "왜 이렇게 했어?" 캐묻기

5. 구현    acceptEdits 모드로 속도

6. 검증    hooks 자동 실행 (compile + test) + ArchUnit

7. 기록    커밋 메시지 Catch: 줄
           예측표 결과 칸 채우기

8. /clear
```

#### WAT 매핑

| | 이 프로젝트 |
|---|---|
| **Workflow** (지침) | `CLAUDE.md`, `docs/specs/`, `docs/workflow.md`, 슬래시 커맨드 |
| **Agent** (판단 주체) | Claude Code 본체 — **서브에이전트 없음** (근거는 9.A.1) |
| **Tools** (실행 도구) | ArchUnit, JUnit, AssertJ, Gradle, hooks |

---

### 9.D 증거가 남는 곳

| 위치 | 내용 |
|---|---|
| `CLAUDE.md` 커밋 히스토리 | 언제 왜 규칙이 추가됐는지 |
| `docs/decisions/` | 선택지 둘 중 왜 이것을 골랐는지 |
| `docs/ai-predictions.md` | 예측과 적중 여부 |
| `docs/workflow.md` | 작업 순서 설계 |
| `docs/ai-defects/` | 실패 사례 |
| 커밋 `Catch:` 줄 | 위 전부를 잇는 실 |

---

### 9.E README 구성

**AI 섹션을 맨 앞에 두지 않는다.** 제품 소개가 먼저, AI는 그 아래.
순서가 뒤집히면 "혼자서는 못 만들었구나"로 읽힌다.

```
1. 무엇을 만들었나 (스크린샷 1~2장)
2. 왜 만들었나 (유사 서비스가 있지만 X 때문에)
3. 핵심 계산 로직 (기여도 분해 설명)
4. AI를 어떻게 통제했나        ← 아래 3줄
5. 배운 것
```

4번 섹션은 이 정도면 충분하다. 산문으로 길게 쓰지 않는다.

```
### 워크플로우 설계
반복 단위(지표 추가)를 식별해 8단계로 고정 → docs/workflow.md

### 실패 예측
구현 전 AI가 틀릴 지점 5개 예측, 적중 N/5 → docs/ai-predictions.md

### 도입하지 않은 것
서브에이전트, Skills, 하네스 — 근거는 docs/decisions/00-ai-harness.md
```

**세 번째 항목이 있는 README는 드물다.** 대부분 "썼다"만 적는다.

여기에 AI 판단 기록표를 덧붙인다.

| AI가 제안한 것 | 왜 안 받아들였나 | 대신 어떻게 했나 |
|---|---|---|
| (작업 중 하루 한 줄씩 채울 것) | | |

#### 하지 말 것

| | 이유 |
|---|---|
| 도구 나열 ("hooks, plan mode를 활용") | "써봤다"이지 "잘 쓴다"가 아님. 각각 **왜 필요했는지**가 붙어야 함 |
| 대화 로그 첨부 | 아무도 읽지 않음 |
| AI 활용률 같은 수치 | 검증 불가, 의미 없음 |
| "AI로 만들었습니다"를 자랑처럼 | 이제 모두가 그러함 |

#### 면접 대비

"AI 어떻게 쓰셨어요?"는 거의 확정으로 나온다. 한 발 더 나간 답을 준비할 것.

- "AI가 틀린 걸 어떻게 알았나요?" → 도메인을 먼저 조사하고 테스트로 고정했다
- "테스트 없이는 못 잡았을 텐데요?" → 계산 로직은 테스트 우선, ArchUnit으로 타입까지 강제했다
- "AI 없이도 만들 수 있었나요?" → 시간이 더 걸렸겠지만 만들 수 있었다. 계산 로직 설계는 내가 했고 AI는 구현 속도를 올렸다

> **마지막 답이 중요하다. "AI가 없으면 못 만든다"는 인상만 피하면 된다.**

## 10. 일정 (2주)

| 기간 | 내용 |
|---|---|
| **Day 1** | ⚠️ API 확인 (배당 이력 + **주가 시계열** + 배당성향) / 저장소 / CLAUDE.md / Hooks / ArchUnit / **커밋 규칙 시작** |
| **Day 2** | 지표 정의 확정, 배당 규칙 조사, **테스트 먼저 작성**, `docs/ai-predictions.md` 작성, `docs/workflow.md` 작성 |
| **Day 3~4** | **빈 껍데기 배포** (기능 없어도 URL이 열리는지 확인) |
| **Day 5~9** | 계산 엔진 (기여도 분해 → 지속성 지표 → 이상치 제거) — 매 지표마다 8단계 워크플로우 적용 |
| **Day 10~12** | 화면 (목록 / 종목 상세 / 월별 차트) |
| **Day 13~14** | README, 예측표 결과 정리, `00-ai-harness.md` 마무리, 데모 |

### Day 1에 몰아서 끝내야 하는 것 (소급 적용 불가)

1. CLAUDE.md — 이후 갱신 diff가 증거가 되므로 처음부터 있어야 함
2. Hooks — 나중에 걸면 그동안 놓친 실수를 소급 발견할 수 없음
3. 커밋 메시지 규칙 — 처음부터 지켜야 스토리가 재구성됨

나머지(Plan mode, `/clear`)는 습관이므로 비용이 0이다.

### Day 1이 갈림길인 이유

**주가 시계열을 무료로 못 받으면 기여도 분해가 불가능하고, 이 프로젝트의 정체성이 무너진다.**
확인 전까지 나머지 계획은 전부 가정이다. 안 되면 그날 방향을 트는 것이 가장 싸다.

### Day 3~4에 미리 배포하는 이유

마지막 날 배포하려다 실패하는 것이 단기 프로젝트에서 가장 흔한 사고.

---

## 11. 위험 요소와 대응

| 위험 | 대응 |
|---|---|
| 주가 시계열 무료 확보 실패 | Day 1 확인. 실패 시 지속성 지표 중심으로 축소 |
| 배당성향 등 재무 지표 미제공 | 배당 이력만으로 산출 가능한 지표로 대체 |
| 배당킹 지급 월이 특정 월에 편중 | 월배당 종목 소수 추가 검토 (Day 2 판단) |
| API 정책 변경 | 어댑터 격리 + 예비 제공자 확보 |
| 계산 결과가 틀림 | 테스트 우선 작성, 손계산 검증 케이스 확보 |
| 범위 확대로 일정 초과 | 범위 밖 항목을 명시했고, 추가 시 문서에 반영 후 진행 |

---

## 12. 반드시 표시할 고지 사항

돈을 다루는 도구이므로 아래를 UI에 명시한다.

- **투자 조언이 아님.** 정보 제공 목적
- **과거 이력 기반 추정치.** 기업은 배당을 줄이거나 없앨 수 있음
- **주가 변동은 반영하지 않음.** 배당 3%를 받아도 주가가 하락하면 손실
- **세금은 원천징수 15%만 반영.** 금융소득종합과세는 별도
- **데이터 출처 표기**

> 이 고지들은 안전장치인 동시에 어필 포인트다.
> "돈을 다루는 서비스의 위험을 인식하고 설계에 반영했다"는 신입 포트폴리오에서 드문 태도다.

---

## 13. 확장 방향 (2주 이후)

1차 완성 후 실제로 만져보고 나서 결정한다. 지금 확정하지 않는다.

- 지표 항목 확대 (잉여현금흐름 대비 배당, 부채 수준)
- 유니버스 확대 (배당귀족, 월배당 종목)
- 월 균등 배당 조합 계산
- 설명 문장 품질 개선 (LLM 활용 — 단, **판정은 코드, 서술만 LLM**)

---

## 14. 최종 산출물 목록

프로젝트가 끝났을 때 저장소에 있어야 하는 것.

```
├── CLAUDE.md                        규칙 (커밋 히스토리가 증거)
├── README.md                        제품 소개 + AI 섹션 3줄 + 판단 기록표
├── .claude/
│   ├── settings.json                hooks
│   └── commands/                    /spec, /defect, /verify
├── docs/
│   ├── workflow.md                  지표 추가 8단계
│   ├── ai-predictions.md            사전 예측과 적중 여부
│   ├── decisions/
│   │   ├── 00-ai-harness.md         도입/미도입 판단 근거
│   │   ├── 01-data-source.md        API 선정 과정
│   │   ├── 02-universe.md           유니버스 선정 근거
│   │   └── 03-*.md                  지표별 설계 결정
│   ├── specs/                       지표별 스펙
│   └── ai-defects/                  실패 사례
└── src/
    ├── main/                        계산 엔진(두껍게) + 표현 계층(얇게)
    └── test/                        손계산 검증 케이스 포함
```

---

## 15. 검증되지 않은 가정 (Day 1~2에 확인)

이 문서에서 아직 사실로 확정되지 않은 것들:

- [ ] Finnhub 무료 플랜에서 배당 이력의 `paymentDate` 가 실제로 채워지는가
- [ ] 주가 시계열을 무료로 충분한 기간 받을 수 있는가
- [ ] 배당성향 등 재무 지표를 무료로 받을 수 있는가
- [ ] Finnhub 현재 약관이 공개 배포를 허용하는가
- [ ] 배당킹 57개 목록을 어디서 확보할 것인가 (수동 입력 시 출처 표기)
- [ ] 배당킹의 실제 지급 월 분포 (특정 월 편중 여부)
- [ ] 유사 서비스 중 "지표 분해"를 하는 곳이 정말 없는가 (30분 재조사)

**확인 결과는 각각 `docs/decisions/` 에 기록한다.**
