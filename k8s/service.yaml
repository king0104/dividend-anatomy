apiVersion: v1
kind: Service
metadata:
  name: dividend-anatomy
  namespace: dividend-anatomy
spec:
  type: NodePort
  selector:
    app: dividend-anatomy
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
